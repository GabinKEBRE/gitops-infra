- name: Bootstrap servers
  hosts: all
  become: true

  tasks:

    - name: Update packages
      package:
        name: "*"
        state: latest

    - name: Install basic tools
      package:
        name:
          - curl
          - vim
          - git
        state: present
